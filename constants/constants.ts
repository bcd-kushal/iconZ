// =========================================================================================================
// =====[ CONSTANTS ]======================================================================================
// =========================================================================================================

export const PORT = 5001
export const MAX_REQUEST_LENGTH = 20
export const API_ROUTE = {
    ALL_SUPPORTED_LIST: "/supported-list",
    PRIVATE_USE_ENDPOINT: "/api",
    PUBLIC_USE_ENDPOINT: ""
}

